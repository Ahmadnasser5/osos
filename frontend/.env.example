# Leave unset for local dev — Vite's dev server proxies /api to
# http://localhost:5000 automatically (see vite.config.js).
#
# For production on Vercel, set this in the Vercel project's
# Environment Variables to your deployed Render backend URL
# (no trailing slash):
# VITE_API_URL=https://your-app.onrender.com
